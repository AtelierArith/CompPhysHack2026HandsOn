# quarto-slide-example

## Description

How to view slides locally

```sh
$ cd path/to/this/directory
$ quarto preview
```

- `_quarto.yml` にある `title` を変更する
- `index.qmd` にある `title` を変更
- `index.qmd` にある `Description` を必要に応じて変更する

